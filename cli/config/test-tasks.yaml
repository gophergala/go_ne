tasks:
  setup:
    steps:
      - plugin: bootstrap
        args:
          - "git"
      - plugin: git-clone
        args:
          - "https://github.com/Tobscher/go-three.git"
  deploy:
    steps:
      - command: /usr/bin/whoami
      - plugin: env
